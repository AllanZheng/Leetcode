package main

func maxProfit(prices []int, fee int) int {
	length := len(prices)
	dp := make([][]int, length)
	for i := range dp {
		dp[i] = make([]int, 2)
	}
	for i := 1; i < length; i++ {

	}
}
func main() {

}
